enum LoginStatus { initail, loading, success, error }

enum UploadStatus { initail, loading, success, error }

enum RegisterStatus { initail, loading, success, error }

enum HomePageStatus { initail, loading, success, error }

enum TeacherPageStatus {initial,loading,success,error}

enum DocumentListPageStatus {initial,loading,success,error}

enum StudentListPageStatus {initial,loading,success,error}

enum TeacherListPageStatus {initial,loading,success,error}

enum DocumentStatus { pending, approved, rejected }
